name: CI

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  backend:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Install (backend)
        working-directory: backend
        run: npm ci

      - name: Syntax check
        run: |
          node --check backend/server.js
          node --check backend/app.js
          node --check backend/config/database.js
          node --check backend/middleware/securityHeaders.js
          node --check backend/middleware/validate.js
          node --check backend/routes/auth.js
          node --check backend/routes/contact.js
          node --check backend/routes/inventory.js
          node --check backend/routes/locations.js
          node --check backend/routes/passwordReset.js
          node --check backend/routes/products.js
          node --check backend/routes/returns.js

      - name: Smoke test (backend)
        working-directory: backend
        run: node scripts/smoke-ci.js

  frontend:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install (frontend)
        working-directory: frontend
        run: npm ci

      - name: Test (frontend)
        working-directory: frontend
        env:
          CI: "true"
        run: npm test -- --watchAll=false

      - name: Build (frontend)
        working-directory: frontend
        env:
          CI: "true"
        run: npm run build
